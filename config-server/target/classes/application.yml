server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ale0790/floricultura-config-repository
          default-label: main
          search-paths: fornecedor
